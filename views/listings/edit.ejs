<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust</title>
</head>
<body>
    <h2>create a new listing</h2>
    <form method="post" action="/listings/<%= listingEach._id %>?_method=PUT" >
        <input name="listing[title]" value="<%= listingEach.title %>" type="text" >
        <br/>
        <br/>
        <textarea  name="listing[description]"  ><%= listingEach.description %></textarea>
        <br/>

        <input name="listing[image]" value="<%= listingEach.image %>" type="text" >
        <br/>
        <br/>
        <input name="listing[price]" value="<%= listingEach.price %>" type="number" >
        <br/>
        <br/>
        <input name="listing[country]" value="<%= listingEach.country %>" type="text" >
        <br/>
        <br/>
        <input name="listing[location]" value="<%= listingEach.location %>" type="text" >
        <br/>
        <br/>
        <button>edit</button>
    </form>
</body>
</html>